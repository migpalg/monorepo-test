{
  "name": "@migpalg/pack",
  "version": "0.1.0",
  "main": "build/index.js",
  "license": "MIT",
  "scripts": {
    "build:clean": "rm -rf dist",
    "build:commonjs": "echo \"not implemented yet\".",
    "build:types": "tsc -b ./tsconfig.build.json",
    "build": "yarn build:clean && yarn build:commonjs && pnpm build:types"
  },
  "dependencies": {
    "@migpalg/core": "^0.1.0"
  }
}
